'use client';

import { AndroidMockup } from '@/components/android/AndroidMockup';

export default function Home() {
  return (
    <main className="flex items-center justify-center min-h-screen w-full bg-background p-4 overflow-hidden">
      <AndroidMockup />
    </main>
  );
}
